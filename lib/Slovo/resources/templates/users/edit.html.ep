% my $title = 'Промѣна на потрѣбителска сметка';
% layout 'upravlenie', title => $title;
<h2><%= $title %></h2>
%= include 'users/_form', caption => 'Update', target => 'update_users'
%#= button_to Remove => remove_users => {id => $users->{id}}
