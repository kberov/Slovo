% my $title = 'Прѣгледъ  на потребителска сметка на '. $users->{login_name};
% layout 'upravlenie', title => $title;
<h2><%= $title %></h2>

<hr>
% for my $k (keys %$users) {
<%== t span => (id =>$k, class => 'item-field')
    => "$k: " . ($users->{$k} // '') %>
% }
<hr>

%= link_to 'Edit' => edit_users => {id => $users->{id}}

